sudo: required
services:
  - docker

before_install:
  - docker build -t kristinakanatso/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run kristinakanatso/react-test npm test -- --coverage

after_success:
  - docker build -t kristinakanatso/multi-client ./client
  - docker build -t kristinakanatso/multi-nginx ./nginx
  - docker build -t kristinakanatso/multi-server ./server
  - docker build -t kristinakanatso/multi-worker ./worker
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  - docker push kristinakanatso/multi-client
  - docker push kristinakanatso/multi-nginx
  - docker push kristinakanatso/multi-server
  - docker push kristinakanatso/multi-worker

